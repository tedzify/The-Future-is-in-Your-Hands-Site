function showCandidate(category, candidateNumber) {
    // This function will alert the category and candidate number when the button is clicked
    alert('You clicked on ' + category + ' Candidate ' + candidateNumber);
  }
  